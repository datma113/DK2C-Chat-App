const initial = [];

const reducer = (state = initial, action) => {
    let { type, users } = action;
    switch (type) {
    
        default:
            break;
    }

    return state;
};

export default reducer;
